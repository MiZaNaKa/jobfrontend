import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    dualTextInputConatiner:{
        flexDirection: 'row', 
        width: '100%',
        backgroundColor: '#FFFFFF'}
  });

export default styles;